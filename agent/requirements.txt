httpx==0.27.2

# Phase 2 — browser agent. Install with: pip install playwright && playwright install chromium
playwright>=1.49,<2

# Phase 3 — AI test agent. Requires ANTHROPIC_API_KEY at runtime.
anthropic==0.39.0
